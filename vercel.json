{
  "version": 2,
  "buildCommand": "npm install && cd site && npm install && npm run build",
  "outputDirectory": "site/dist",
  "framework": "express",
  "functions": {
    "api/index.js": {
      "memory": 128,
      "maxDuration": 10
    }
  }
}
