{
  "name": "popthequestion",
  "version": "0.0.1",
  "dependencies": {
    "async": "^0.9.0",
    "coffee-script": "~1.8.0",
    "express": "~4.10.4"
  },
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node server.js",
    "postinstall": "cd site && npm install",
    "build": "cd site && npm run build"
  }
}
